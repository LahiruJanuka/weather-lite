# 🌤️ Weather-Lite Dashboard — JavaScript Fundamentals

> A hands-on project to solidify core **JavaScript fundamentals** by building a simple, functional weather dashboard using **Vanilla JS** and a real-world API.

---

## 🚀 Project Overview

This project focuses on practicing modern JavaScript concepts such as **asynchronous programming**, **API interaction**, and **dynamic DOM manipulation**. It integrates the **OpenWeatherMap API** to fetch and display live weather data in the browser.

The goal is learning-by-doing: writing clean JavaScript, working with external data, and updating the UI dynamically.

---

## 🌟 Features & Learning Outcomes

Built with **HTML**, **CSS**, and **Vanilla JavaScript**, this project demonstrates practical, real-world JavaScript skills:

* **Asynchronous Programming (`async/await`)**
  Clean handling of asynchronous operations such as API requests and responses.

* **API Integration (OpenWeatherMap)**
  Fetching live weather data from a third-party REST API using query parameters and API keys.

* **Robust Error Handling**
  Graceful handling of invalid input, network errors, and failed HTTP responses.

* **JSON Parsing & Destructuring**
  Extracting nested data from API responses using modern JavaScript destructuring with safe defaults.

* **Dynamic DOM Manipulation**
  Creating, updating, and clearing UI elements dynamically based on fetched data.

* **Input Sanitization**
  Cleaning user input to prevent invalid API requests and improve reliability.

* **Date & Timezone Formatting**
  Converting UNIX timestamps and timezones into readable local date and time using `Intl.DateTimeFormat`.

---

## 🛠️ Technologies Used

| Technology        | Purpose                                     |
| ----------------- | ------------------------------------------- |
| HTML5             | Structure and semantic markup               |
| CSS3              | Styling and layout                          |
| JavaScript        | Core logic, API calls, and DOM manipulation |

---

## 📂 Project Structure

```text
├── index.html      # Main HTML structure
├── styles.css       # Project styling
└── index.js       # JavaScript logic (API calls, data handling, DOM updates)
```

---

## 💻 Getting Started

Follow these steps to run the project locally:

### 1️⃣ Clone the Repository

```bash
git clone https://github.com/LahiruJanuka/weather-lite.git
```

### 2️⃣ Get an API Key

* Create an account on **OpenWeatherMap**
* Generate your personal API key

### 3️⃣ Configure the API Key

Open `index.js` and replace the placeholder with your key:

```javascript
const apikey = "YOUR_API_KEY_HERE"; // Replace with your API key
```

### 4️⃣ Run the Project

Simply open `index.html` in your web browser.

---

## This project is intended for learning and experimentation.

Happy coding! 🚀
